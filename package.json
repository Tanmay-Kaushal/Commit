{
  "name": "commit-app",
  "private": true,
  "version": "1.2.0",
  "description": "Single deployable entry point: builds the frontend into backend/public and runs the backend, which serves both. One Railway service, root directory = repo root.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/src/index.js",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
