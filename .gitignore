node_modules/

# Frontend build output — generated into backend/public by `npm run build`
# (see vite.config.ts), not source.
backend/public/

# Local SQLite database
backend/dev.db
backend/dev.db-shm
backend/dev.db-wal

# Env files with secrets — .env.example (if you add one) stays tracked
.env
backend/.env
frontend/.env
